# 1. 설치

설치 방법은 여러가지 형태가 있지만 심플하게 도커로 구성하려고 한다.

#### 설치 문서

https://www.mongodb.com/ko-kr/docs/manual/tutorial/install-mongodb-community-with-docker/#std-label-docker-mongodb-community-install



## 절차

### 1. MongoDB Docker 이미지 가져오기

```
docker pull mongodb/mongodb-community-server:latest
```



### 2. 이미지를 컨테이너로 실행

```
docker run --name mongodb -p 27017:27017 -d mongodb/mongodb-community-server:latest
```

명령의 `-p 27017:27017`가 컨테이너 포트를 호스트 포트에 매핑합니다. 이렇게 하면 `localhost:27017` 연결 문자열을 사용하여 MongoDB에 연결할 수 있습니다.

특정 버전의 MongoDB를 설치하려면 Docker 실행 명령에서 `:` 뒷부분에 버전을 지정합니다. Docker는 지정된 버전을 가져와 실행합니다.

예를 들어 MongoDB 5.0을 실행하려면 다음을 실행합니다.

```
docker run --name mongodb -p 27017:27017 -d mongodb/mongodb-community-server:5.0-ubuntu2004
```

사용 가능한 버전의 전체 목록은 [태그](https://hub.docker.com/r/mongodb/mongodb-community-server/tags)에서 확인 가능합니다.

## 참고

> ### **명령줄 옵션 추가**
>
> Docker 실행 명령에 명령줄 옵션을 추가하여 [mongod 명령줄 옵션](https://www.mongodb.com/ko-kr/docs/manual/reference/configuration-file-settings-command-line-options-mapping/#std-label-conf-file-command-line-mapping)을 사용할 수 있습니다.
>
> 예를 들어 [`mongod --replSet`](https://www.mongodb.com/ko-kr/docs/manual/reference/program/mongod/#std-option-mongod.--replSet) Docker 명령줄 옵션을 생각할 수 있습니다.
>
> ```
> docker run -p 27017:27017 -d mongodb/mongodb-community-server:latest --name mongodb --replSet myReplicaSet
> ```



### 3. 컨테이너 실행 확인

Docker container의 상태를 확인하려면 다음 명령을 실행합니다.

```
docker container ls
```

`ls` 명령의 출력에는 실행 중인 container를 설명하는 다음 필드가 나열됩니다.

- `Container ID`
- `Image`
- `Command`
- `Created`
- `Status`
- `Port`
- `Names`

```
CONTAINER ID   IMAGE                                       COMMAND                  CREATED         STATUS         PORTS       NAMES
c29db5687290   mongodb/mongodb-community-server:5.0-ubi8   "docker-entrypoint.s…"   4 seconds ago   Up 3 seconds   27017/tcp   mongo
```



### 4. `mongosh`을(를) 사용하여 MongoDB 배포서버에 연결합니다.

```
mongosh --port 27017
```



#### mongosh 설치법

해당 오류가 발생하면 아래 내용을 수행하면 됩니다.

```cmd
mongosh --port 27017
'mongosh'은(는) 내부 또는 외부 명령, 실행할 수 있는 프로그램, 또는
배치 파일이 아닙니다.
```

##### Mongosh 설치 문서

https://www.mongodb.com/ko-kr/docs/mongodb-shell/install/



##### MSI에서 설치

1. MongoDB Shell 다운로드 페이지를 엽니다.
   [MongoDB 다운로드 센터](https://www.mongodb.com/try/download/shell?jmp=docs)를 엽니다.

2. **Platform** 드롭다운 메뉴에서 **Windows 64-bit (8.1+) (MSI)**를 선택합니다.
2. **Download**를 클릭합니다.

4. 설치 프로그램 파일을 두 번 클릭합니다.

5. 메시지에 따라 `mongosh`을 설치합니다.



정상적으로 설치 후 다시 명령어 수행하면 이와같이 나오면 되는 것이다.

```cmd
mongosh --port 27017
Current Mongosh Log ID: 6757f4694dd23e7dbf893bf7
Connecting to:          mongodb://127.0.0.1:27017/?directConnection=true&serverSelectionTimeoutMS=2000&appName=mongosh+2.3.4
Using MongoDB:          7.0.15
Using Mongosh:          2.3.4

For mongosh info see: https://www.mongodb.com/docs/mongodb-shell/


To help improve our products, anonymous usage data is collected and sent to MongoDB periodically (https://www.mongodb.com/legal/privacy-policy).
You can opt-out by running the disableTelemetry() command.

------
   The server generated these startup warnings when booting
   2024-12-10T07:47:59.747+00:00: Using the XFS filesystem is strongly recommended with the WiredTiger storage engine. See http://dochub.mongodb.org/core/prodnotes-filesystem
   2024-12-10T07:48:00.502+00:00: Access control is not enabled for the database. Read and write access to data and configuration is unrestricted
   2024-12-10T07:48:00.502+00:00: /sys/kernel/mm/transparent_hugepage/enabled is 'always'. We suggest setting it to 'never' in this binary version
   2024-12-10T07:48:00.502+00:00: vm.max_map_count is too low
------

test>
```



### 5. 배포서버 유효성 검사

MongoDB 인스턴스가 실행 중인지 확인하려면 `Hello` 명령을 실행합니다.

```
db.runCommand(
   {
      hello: 1
   }
)
```

이 명령의 결과는 `mongod` 배포서버를 설명하는 문서를 반환합니다.

```
{
   isWritablePrimary: true,
   topologyVersion: {
      processId: ObjectId("63c00e27195285e827d48908"),
      counter: Long("0")
},
   maxBsonObjectSize: 16777216,
   maxMessageSizeBytes: 48000000,
   maxWriteBatchSize: 100000,
   localTime: ISODate("2023-01-12T16:51:10.132Z"),
   logicalSessionTimeoutMinutes: 30,
   connectionId: 18,
   minWireVersion: 0,
   maxWireVersion: 20,
   readOnly: false,
   ok: 1
}
```

## 다음 단계(선택 사항)

[Cosign](https://github.com/sigstore/cosign)을 사용하여 컨테이너 이미지에 대한 MongoDB 서명을 확인할 수 있습니다.

이 절차는 선택 사항입니다. Docker 또는 기타 컨테이너화 플랫폼에서 MongoDB를 실행하기 위해 MongoDB의 서명을 확인하지 않아도 됩니다.

MongoDB의 container 서명을 확인하려면 다음 단계를 수행합니다.



### 1. Cosign 다운로드 및 설치

설치 지침은 [Cosign GitHub 리포지토리](https://github.com/sigstore/cosign)에서 확인 가능합니다.



### 2. MongoDB Server 컨테이너 이미지의 공개 키 다운로드

```
curl https://cosign.mongodb.com/server.pem > server.pem
```



### 3. 서명 확인

다음 명령을 실행하여 태그별 서명을 확인합니다.

```
COSIGN_REPOSITORY=docker.io/mongodb/signatures cosign verify --private-infrastructure --key=./server.pem docker.io/mongodb/mongodb-community-server:latest
```



# 2. 스프링 연동

문서는 너무 방대해서 필요한 내용이 있는 경우에만 딥하게 찾아보면 좋을거 같음

일단은 gpt를 통해서 구현



#### build.gradle.kts

```
import org.jetbrains.kotlin.gradle.tasks.KotlinCompile

plugins {
    id("org.springframework.boot") version Versions.springBoot
}

dependencies {

    implementation("org.springframework.boot:spring-boot-starter-web")

    // mongodb setting
    implementation("org.springframework.boot:spring-boot-starter-data-mongodb")


    implementation("org.springframework.boot:spring-boot-starter-actuator")

    testImplementation("org.springframework.boot:spring-boot-starter-test")
}

```



#### application.yml

```
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/test #driver명://호스트명:포트/디비명

# 해당 설정까지는 필수 사항은 아님
logging: 
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
    org.springframework.data.mongodb.repository.query.MongoQueryMethod: DEBUG
    com.mongodb.Mongo: DEBUG

```



#### Document - User

```
package com.example.blog.mongodb.api.data.document

import org.springframework.data.annotation.Id
import org.springframework.data.mongodb.core.mapping.Document

@Document(collection = "users")
data class User(
    @Id
    val id: String? = null,
    val name: String,
    val email: String
)

```



#### Repository - User

```
package com.example.blog.mongodb.api.data

import com.example.blog.mongodb.api.data.document.User
import org.springframework.data.mongodb.repository.MongoRepository

interface UserRepository : MongoRepository<User, String> {
    fun findByName(name: String): List<User>
}

```



#### Service - UserService

```
package com.example.blog.mongodb.api.service

import com.example.blog.mongodb.api.data.UserRepository
import com.example.blog.mongodb.api.data.document.User
import org.springframework.stereotype.Service

@Service
class UserService(private val userRepository: UserRepository) {

    fun getAllUsers(): List<User> = userRepository.findAll()

    fun getUserById(id: String): User? = userRepository.findById(id).orElse(null)

    fun getUsersByName(name: String): List<User> = userRepository.findByName(name)

    fun createUser(user: User): User = userRepository.save(user)

    fun updateUser(id: String, updatedUser: User): User? {
        val existingUser = userRepository.findById(id).orElse(null)
        return if (existingUser != null) {
            val newUser = existingUser.copy(
                name = updatedUser.name,
                email = updatedUser.email
            )
            userRepository.save(newUser)
        } else {
            null
        }
    }

    fun deleteUser(id: String): Boolean {
        return if (userRepository.existsById(id)) {
            userRepository.deleteById(id)
            true
        } else {
            false
        }
    }
}

```



#### Controller - UserController

```
package com.example.blog.mongodb.api.controller

import com.example.blog.mongodb.api.data.document.User
import com.example.blog.mongodb.api.service.UserService
import org.springframework.http.ResponseEntity
import org.springframework.web.bind.annotation.*

@RestController
@RequestMapping("/api/users")
class UserController(private val userService: UserService) {

    @GetMapping
    fun getAllUsers(): ResponseEntity<List<User>> {
        val users = userService.getAllUsers()
        return ResponseEntity.ok(users)
    }

    @GetMapping("/{id}")
    fun getUserById(@PathVariable id: String): ResponseEntity<User> {
        val user = userService.getUserById(id)
        return if (user != null) {
            ResponseEntity.ok(user)
        } else {
            ResponseEntity.notFound().build()
        }
    }

    @GetMapping("/search")
    fun getUsersByName(@RequestParam name: String): ResponseEntity<List<User>> {
        val users = userService.getUsersByName(name)
        return ResponseEntity.ok(users)
    }

    @PostMapping
    fun createUser(@RequestBody user: User): ResponseEntity<User> {
        val newUser = userService.createUser(user)
        return ResponseEntity.ok(newUser)
    }

    @PutMapping("/{id}")
    fun updateUser(
        @PathVariable id: String,
        @RequestBody updatedUser: User
    ): ResponseEntity<User> {
        val user = userService.updateUser(id, updatedUser)
        return if (user != null) {
            ResponseEntity.ok(user)
        } else {
            ResponseEntity.notFound().build()
        }
    }

    @DeleteMapping("/{id}")
    fun deleteUser(@PathVariable id: String): ResponseEntity<Void> {
        return if (userService.deleteUser(id)) {
            ResponseEntity.noContent().build()
        } else {
            ResponseEntity.notFound().build()
        }
    }
}

```



# 3. CRUD 수행



### 1차적으로 Sprint에서 지원하는 Document 형태로 사용

User class와 같이 @Document로 하면 내가 별다른거 없이 Collection 설정도 한번에 되고 해당 객체를 저장할때 BSON 형태로 저장이 잘 되었다.

| _id                      | _class                                          | email                | name   |
| ------------------------ | ----------------------------------------------- | -------------------- | ------ |
| 6759213c1a6c591c15752e3f | com.example.blog.mongodb.api.data.document.User | john.doe@example.com | 염석현 |
| 6759235ade4d4c38f3973e18 | com.example.blog.mongodb.api.data.document.User | john.doe@example.com | 염석현 |



근데 이와같이 하면 `_class` 라는게 생기는데, bson 형태를 객체로 변환할때 사용하는 것이라고 한다.

그리고 id를 따로 지정안하면 MongoDB에서 지원하는 objectId로 저장이 되는것으로 확인했다.



조회나 수정도 어려움 없이 잘 되었다.

```
GET http://localhost:8080/api/users/search?name=염석현

HTTP/1.1 200 
Content-Type: application/json
Transfer-Encoding: chunked
Date: Wed, 11 Dec 2024 05:38:44 GMT
Keep-Alive: timeout=60
Connection: keep-alive

[
  {
    "id": "6759213c1a6c591c15752e3f",
    "name": "염석현",
    "email": "john.doe@example.com"
  },
  {
    "id": "6759235ade4d4c38f3973e18",
    "name": "염석현",
    "email": "john.doe@example.com"
  }
]
```



#### _class 옵션 비활성화

프로젝트 성격에 다르겠지만 내가 원하는건 동적으로 필드를 추가하고, 동적으로 필드를 조회하는 것이다.

그러나 이와같이 타입이 미리 정의가 되어 있으면 객체화 되어 있어 확인은 용의하나, 동적 처리가 힘들어 져서

`_class`가 DB에 저장될때 비활성화 처리를 하였다.

```
package com.example.blog.mongodb.infra.config

import org.springframework.context.annotation.Bean
import org.springframework.context.annotation.Configuration
import org.springframework.data.mongodb.MongoDatabaseFactory
import org.springframework.data.mongodb.config.AbstractMongoClientConfiguration
import org.springframework.data.mongodb.core.convert.DefaultMongoTypeMapper
import org.springframework.data.mongodb.core.convert.MappingMongoConverter
import org.springframework.data.mongodb.core.convert.MongoCustomConversions
import org.springframework.data.mongodb.core.mapping.MongoMappingContext

@Configuration
class MongoConfig : AbstractMongoClientConfiguration() {
    override fun getDatabaseName(): String = "test"

    @Bean
    override fun mappingMongoConverter(
        databaseFactory: MongoDatabaseFactory,
        customConversions: MongoCustomConversions,
        context: MongoMappingContext
    ): MappingMongoConverter {
        val converter = super.mappingMongoConverter(databaseFactory, customConversions, context)
        converter.setTypeMapper(DefaultMongoTypeMapper(null)) // `_class` 필드 제거
        return converter
    }
}

```



# 4. 복잡한 조회 로직

### 1차 Document를 사용한 Vehicle 저장 방법

Vehicle이라는 Document를 생성해서 Deps가 깊은 객체를 만들어서 테스트 하였으나,

생각보다 제어하는게 어려웠다.

```
package com.example.blog.mongodb.api.service

import com.example.blog.mongodb.api.data.document.*
import org.junit.jupiter.api.BeforeEach
import org.junit.jupiter.api.Test
import org.junit.jupiter.api.extension.ExtendWith
import org.springframework.beans.factory.annotation.Autowired
import org.springframework.boot.test.context.SpringBootTest
import org.springframework.data.mongodb.core.MongoTemplate
import org.springframework.data.mongodb.core.query.Criteria
import org.springframework.data.mongodb.core.query.Query
import org.springframework.test.context.junit.jupiter.SpringExtension

@ExtendWith(SpringExtension::class)
@SpringBootTest
class VehicleServiceTest {

    @Autowired
    lateinit var mongoTemplate: MongoTemplate

    // 테스트용 데이터 세팅
    @BeforeEach
    fun setUp() {
        // MongoDB에 미리 차량 데이터를 삽입합니다.
        val vehicle = Vehicle(
            id = "vehicle_test2",
            chassis = Chassis(
                axle = Axle(
                    row2 = Row2(
                        wheel = Wheel(
                            right = Right(
                                speed = 120,
                                brake = Brake(
                                    fluidLevel = 801,
                                    isFluidLevelLow = false,
                                    padWear = 10,
                                    isBrakesWorn = false
                                )
                            )
                        )
                    )
                )
            )
        )
        mongoTemplate.save(vehicle, "vehicles")
    }

    @Test
    fun `Verify data exists`() {
        val count = mongoTemplate.count(Query(), "vehicles")
        println("Total documents in 'vehicles' collection: $count")
    }

    @Test
    fun `MongoTemplate ID 조회 테스트`() {
        val vehicleId = "vehicle_test"
        val query = Query(Criteria.where("id").`is`(vehicleId))
        val result = mongoTemplate.findOne(query, Vehicle::class.java)
        println(result)
    }
}

```



### 2차로는 Json과 Document 형태로 디비에 저장 후 동적 조회를 구현

2가지 형태의 데이터 insert는 매력적이지만, 확실히 json 형태가 편해 보였다.



그리고 `@Document` 객체를 쓰는게 아니다 보니 문법이 살짝 달라졌다.

기존 `id` 조회 할때는 그냥 id만 사용하면 되었지만, 이제는 `_id`로 해야 조회가 되는 것을 알게 되었다.

그리고 내가 원하는 `speed`의 value 값만 얻는거 까지 수행 하였다.



```
package com.example.blog.mongodb.api.service

import org.bson.Document
import org.bson.json.JsonParseException
import org.junit.jupiter.api.BeforeEach
import org.junit.jupiter.api.Test
import org.junit.jupiter.api.extension.ExtendWith
import org.springframework.beans.factory.annotation.Autowired
import org.springframework.boot.test.context.SpringBootTest
import org.springframework.data.mongodb.core.MongoTemplate
import org.springframework.data.mongodb.core.aggregation.Aggregation
import org.springframework.data.mongodb.core.query.Criteria
import org.springframework.data.mongodb.core.query.Query
import org.springframework.test.context.junit.jupiter.SpringExtension

@ExtendWith(SpringExtension::class)
@SpringBootTest
class DynamicFieldTest {

    @Autowired
    lateinit var mongoTemplate: MongoTemplate

    // 테스트용 데이터 세팅
    @BeforeEach
    fun setUp() {
        jsonTypeSave()
    }

    fun jsonTypeSave() {
        // JSON 문자열을 Document로 변환
        val json = """
            {
                "_id": "vehicle_test4",
                "chassis": {
                    "axle": {
                        "row2": {
                            "wheel": {
                                "right": {
                                    "speed": 120,
                                    "brake": {
                                        "fluidLevel": 801,
                                        "isFluidLevelLow": false,
                                        "padWear": 10,
                                        "isBrakesWorn": false
                                    }
                                }
                            }
                        }
                    }
                }
            }
        """

        try {
            val document = Document.parse(json)  // JSON 문자열을 Document로 변환
            mongoTemplate.save(document, "vehicles")
        } catch (e: JsonParseException) {
            println("Failed to parse JSON: ${e.message}")
        }
    }

    fun documentTypeSave() {
        // MongoDB에서 사용하는 특수 타입을 사용하여 데이터 생성
        val vehicle = Document("_id", "vehicle_test3")
            .append(
                "chassis", Document(
                    "axle", Document(
                        "row2", Document(
                            "wheel", Document(
                                "right", Document("speed", 120)
                                    .append(
                                        "brake", Document("fluidLevel", 801)
                                            .append("isFluidLevelLow", false)
                                            .append("padWear", 10)
                                            .append("isBrakesWorn", false)

                                    )
                            )
                        )
                    )
                )
            )

        mongoTemplate.save(vehicle, "vehicles")
    }

    @Test
    fun `Verify data exists`() {
        val count = mongoTemplate.count(Query(), "vehicles")
        println("Total documents in 'vehicles' collection: $count")
    }

    @Test
    fun `MongoTemplate ID 조회 테스트`() {
        val vehicleId = "vehicle_test4"
        val query = Query(Criteria.where("_id").`is`(vehicleId))
        val result = mongoTemplate.findOne(query, Document::class.java, "vehicles")
        println(result)
    }

    @Test
    fun `Test retrieving specific path dynamically using Aggregation`() {
        val vehicleId = "vehicle_test4"
        // 조회할 path 지정
        val path = "chassis.axle.row2.wheel.right"
        val fields = path.split('.')

        // MongoDB에서 _id가 vehicle_test4인 데이터 조회
        val aggregation = Aggregation.newAggregation(
            Aggregation.match(Criteria.where("_id").`is`(vehicleId)),  // ID로 필터링
            Aggregation.project().and(path).`as`(fields.last())
                .andExclude("_id")  // 동적 경로를 이용한 프로젝션
        )

        // Aggregation 실행
        val result = mongoTemplate.aggregate(aggregation, "vehicles", Document::class.java)
        println(result.mappedResults.firstOrNull()?.get(fields.last()))
    }
}

```



# 5. 복잡한 수정 로직

복잡한 수정/추가 로직을 수행 했을때,  원하는 결과로 정상적으로 수행되는것을 확인

```
    @Test
    fun `복장한 수정 로직`() {
        // 업데이트할 조건 (_id가 "vehicle_test4"인 문서)
        val query = Query(Criteria.where("_id").`is`("vehicle_test4"))

        // 업데이트할 필드와 값 ($set 연산 사용)
        val update = Update().set("chassis.axle.row2.wheel.right.brake.isFluidLevelLow", true)

        // 업데이트 실행
        val result = mongoTemplate.updateFirst(query, update, "vehicles")

        // 결과 확인 (성공적으로 업데이트되었는지)
        println("Matched count: ${result.matchedCount}, Modified count: ${result.modifiedCount}")
    }

    @Test
    fun `복잡한 추가 로직`() {
        // 업데이트할 조건 (_id가 "vehicle_test4"인 문서)
        val query = Query(Criteria.where("_id").`is`("vehicle_test4"))

        // 업데이트할 필드와 값 ($set 연산 사용)
        val update = Update().set("chassis.axle.row2.wheel.right.brake.isSpin", true)

        // 업데이트 실행
        val result = mongoTemplate.updateFirst(query, update, "vehicles")

        // 결과 확인 (성공적으로 업데이트되었는지)
        println("Matched count: ${result.matchedCount}, Modified count: ${result.modifiedCount}")
    }
```



#### 수행전

```bson
{
    "axle": {
        "row2": {
            "wheel": {
                "right": {
                    "speed": new NumberInt("120"),
                    "brake": {
                        "fluidLevel": new NumberInt("801"),
                        "isFluidLevelLow": false,
                        "padWear": new NumberInt("10"),
                        "isBrakesWorn": false
                    }
                }
            }
        }
    }
}
```



#### 복잡한 수정 로직 수행 후 결과

```
{
    "axle": {
        "row2": {
            "wheel": {
                "right": {
                    "speed": new NumberInt("120"),
                    "brake": {
                        "fluidLevel": new NumberInt("801"),
                        "isFluidLevelLow": true, // true로 수정된걸 확인
                        "padWear": new NumberInt("10"),
                        "isBrakesWorn": false
                    }
                }
            }
        }
    }
}
```



#### 복잡한 추가 로직 수행 후 결과

```
{
    "axle": {
        "row2": {
            "wheel": {
                "right": {
                    "speed": new NumberInt("120"),
                    "brake": {
                        "fluidLevel": new NumberInt("801"),
                        "isFluidLevelLow": false,
                        "padWear": new NumberInt("10"),
                        "isBrakesWorn": false,
                        "isSpin": true // spin 필드가 추가 된것을 확인
                    }
                }
            }
        }
    }
}
```



# 6. 데이터 위치 변경?

2가지 방식을 사용

1. Aggregation을 활용한 데이터 조회 후 수정
2. getEmbedded를 활용한 데이터 조회 후 수정



둘다 응용방법이 간단하고 로직적으로는 getEmbedded가 좀더 심플한 것으로 보임



```
    @Test
    fun `brake 데이터를 right에서 left로 이동`() {
        val vehicleId = "vehicle_test4"
        val query = Query(Criteria.where("_id").`is`("vehicle_test4"))

        // MongoDB에서 _id가 vehicle_test4인 데이터 조회
        val aggregation = Aggregation.newAggregation(
            Aggregation.match(Criteria.where("_id").`is`(vehicleId)),  // ID로 필터링
            Aggregation.project().and("chassis.axle.row2.wheel.right.brake").`as`("brake")
                .andExclude("_id")  // 동적 경로를 이용한 프로젝션
        )

        // Aggregation 실행
        val result = mongoTemplate.aggregate(aggregation, "vehicles", Document::class.java)

        // brake 데이터를 새로운 경로에 복사
        val setUpdate = Update().set(
            "chassis.axle.row2.wheel.left.brake",
            result.mappedResults.firstOrNull()?.get("brake")
        )

        // 기존 경로에서 brake 데이터 삭제
        val unsetUpdate = Update().unset("chassis.axle.row2.wheel.right.brake")

        // 데이터를 이동시키기 위해 순차적으로 업데이트 수행
        mongoTemplate.updateFirst(query, setUpdate, "vehicles")
        mongoTemplate.updateFirst(query, unsetUpdate, "vehicles")
    }

    @Test
    fun `brake 데이터를 right에서 left로 이동2`() {
        val query = Query(Criteria.where("_id").`is`("vehicle_test4"))

        // brake 데이터를 left 경로로 복사
        val setUpdate = Update().set(
            "chassis.axle.row2.wheel.left.brake",
            mongoTemplate.findOne(query, Document::class.java, "vehicles")
                ?.getEmbedded(listOf("chassis", "axle", "row2", "wheel", "right", "brake"), Document::class.java)
        )

        // 기존 경로의 brake 데이터를 제거
        val unsetUpdate = Update().unset("chassis.axle.row2.wheel.right.brake")

        // 데이터를 이동시키기 위해 순차적으로 업데이트 수행
        mongoTemplate.updateFirst(query, setUpdate, "vehicles")
        mongoTemplate.updateFirst(query, unsetUpdate, "vehicles")
    }
```



# 7. 몽고에서 Json 데이터 사용에 대한 고민

json에 대한 deps가 깊어질 수록 처리하는 방식이 복잡해질 수 있을것으로 생각이 된다.

그래서 고안한 방식은 json은 flat 으로 변경해서 작업하는 것을 고안했다.



collection이 여러개로 나눠졌을때, join하기가 힘든 경우가 있을거 같아,

vss 스펙 기준으로 1deps를 collection으로 나눠서 저장하는 방식을 고안했다.

그리고 collection으로 나눠진 json 자체도 deps가 매우 깊기 때문에 json을 flat으로 변경하는 작업을 해보았다.



아래와 같은 json이 저장

```
{
    "_id": "vehicle_test4",
    "chassis": {
        "axle": {
            "row2": {
                "wheel": {
                    "right": {
                        "speed": 120,
                        "brake": {
                            "fluidLevel": 801,
                            "isFluidLevelLow": false,
                            "padWear": 10,
                            "isBrakesWorn": false
                        }
                    }
                }
            }
        }
    }
}
```

아래와 같이 저장이 된다.

| _id           | chassis                                                      |
| ------------- | ------------------------------------------------------------ |
| vehicle_test4 | {axle={row2={wheel={right={speed=120}, left={brake={fluidLevel=801,  isFluidLevelLow=false, padWear=10, isBrakesWorn=false}}}}}} |

그래서 이와같은 json을 flat화 시켜서 진행하기로 하였다.

```
{
    "_id": "vehicle_test5",
    "chassis": {
        "axle": {
            "row2": {
                "wheel": {
                    "right": {
                        "speed": 120,
                        "brake": {
                            "fluidLevel": 801,
                            "isFluidLevelLow": false,
                            "padWear": 10,
                            "isBrakesWorn": false
                        }
                    }
                }
            }
        }
    }
}
```

`.`으로 저장할때는, 몽고디비에서 저장할때 오류가 발생...

```
{
    "_id": "vehicle_test4",
    "chassis.axle.row2.wheel.right.speed": "120",
    "chassis.axle.row2.wheel.right.brake.fluidLevel": "801",
    "chassis.axle.row2.wheel.right.brake.isFluidLevelLow": "false",
    "chassis.axle.row2.wheel.right.brake.padWear": "10",
    "chassis.axle.row2.wheel.right.brake.isBrakesWorn": "false"
}
```

오류 내용

```
org.springframework.data.mapping.MappingException: Map key chassis.axle.row2.wheel.right.speed contains dots but no replacement was configured! Make sure map keys don't contain dots in the first place or configure an appropriate replacement!
```



그래서 아래와 같은 형식으로 `-` 대시로 구분 지어서 저장했을때, 정상적으로 저장이 되었고 

```
{
    "_id": "vehicle_test5",
    "chassis-axle-row2-wheel-right-speed": "120",
    "chassis-axle-row2-wheel-right-brake-fluidLevel": "801",
    "chassis-axle-row2-wheel-right-brake-isFluidLevelLow": "false",
    "chassis-axle-row2-wheel-right-brake-padWear": "10",
    "chassis-axle-row2-wheel-right-brake-isBrakesWorn": "false"
}
```

이와같은 형식으로 저장이 되었다.

| _id           | chassis-axle-row2-wheel-right-brake-fluidLevel | chassis-axle-row2-wheel-right-brake-isBrakesWorn | chassis-axle-row2-wheel-right-brake-isFluidLevelLow | chassis-axle-row2-wheel-right-brake-padWear | chassis-axle-row2-wheel-right-speed |
| ------------- | ---------------------------------------------- | ------------------------------------------------ | --------------------------------------------------- | ------------------------------------------- | ----------------------------------- |
| vehicle_test5 | 801                                            | false                                            | false                                               | 10                                          | 120                                 |



이게 정답인지는 모르겠지만, 원하는 데이터를 쉽게 조회는 할 수 있는 장점이 있을 것으로 보인다.















